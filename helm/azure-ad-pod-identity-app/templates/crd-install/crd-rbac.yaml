apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "aad.crdInstall" . }}
  namespace: {{ .Release.Namespace | quote }}
  annotations:
    # create hook dependencies in the right order
    "helm.sh/hook-weight": "-3"
    {{- include "aad.CRDInstallAnnotations" . | nindent 4 }}
  labels:
    app.kubernetes.io/component: {{ include "aad.crdInstall" . | quote }}
    {{- include "aad.selectorLabels" . | nindent 4 }}
    role: {{ include "aad.CRDInstallSelector" . | quote }}
rules:
- apiGroups:
  - ""
  resources:
  - jobs
  verbs:
  - create
  - delete
- apiGroups:
  - apiextensions.k8s.io
  resources:
  - customresourcedefinitions
  verbs:
  - create
  - delete
  - get
  - patch
- apiGroups:
  - policy
  resources:
  - podsecuritypolicies
  resourceNames:
  - {{ include "aad.crdInstall" . }}
  verbs:
  - use
- apiGroups:
  - apps
  - extensions
  resources:
  - deployments
  verbs:
  - get
  - patch
  - list
- apiGroups:
  - aadpodidentity.k8s.io
  resources:
  - azurepodidentityexceptions
  verbs:
  - get
  - list
  - create
  - delete
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "aad.crdInstall" . }}
  namespace: {{ .Release.Namespace | quote }}
  annotations:
    # create hook dependencies in the right order
    "helm.sh/hook-weight": "-2"
    {{- include "aad.CRDInstallAnnotations" . | nindent 4 }}
  labels:
    app.kubernetes.io/component: {{ include "aad.crdInstall" . | quote }}
    {{- include "aad.selectorLabels" . | nindent 4 }}
    role: {{ include "aad.CRDInstallSelector" . | quote }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "aad.crdInstall" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "aad.crdInstall" . }}
    namespace: {{ .Release.Namespace | quote }}
